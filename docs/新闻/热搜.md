---
title: 今日热搜
---


<!-- 热搜组件 -->
<div class="hot-search-wrapper">
    <div class="hot-header">
        <div class="hot-tabs">

            <!-- 头条：使用报纸/列表图标 -->
            <button class="tab-btn active" data-source="toutiao">
                <svg viewBox="0 0 24 24" width="16" height="16" stroke="currentColor" stroke-width="2" fill="none" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M4 22h16a2 2 0 0 0 2-2V4a2 2 0 0 0-2-2H8a2 2 0 0 0-2 2v16a2 2 0 0 1-2 2Zm0 0a2 2 0 0 1-2-2v-9c0-1.1.9-2 2-2h2"></path>
                    <path d="M18 14h-8"></path>
                    <path d="M15 18h-5"></path>
                    <path d="M10 6h8v4h-8V6Z"></path>
                </svg>
                头条
            </button>

            <!-- 夸克：使用星球/浏览器图标 -->
            <button class="tab-btn" data-source="quark">
                <svg viewBox="0 0 24 24" width="16" height="16" stroke="currentColor" stroke-width="2" fill="none" stroke-linecap="round" stroke-linejoin="round">
                    <circle cx="12" cy="12" r="10"></circle>
                    <line x1="2" y1="12" x2="22" y2="12"></line>
                    <path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"></path>
                </svg>
                夸克
            </button>


            <!-- 抖音：使用音符图标 -->
            <button class="tab-btn" data-source="douyin">
                <svg viewBox="0 0 24 24" width="16" height="16" stroke="currentColor" stroke-width="2" fill="none" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M9 18V5l12-2v13"></path>
                    <circle cx="6" cy="18" r="3"></circle>
                    <circle cx="18" cy="16" r="3"></circle>
                </svg>
                抖音
            </button>
        </div>
        
        <!-- 刷新按钮：也换成 SVG，让旋转更顺滑 -->
        <div class="hot-refresh" onclick="refreshHotSearch()" title="刷新列表">
            <svg viewBox="0 0 24 24" width="18" height="18" stroke="currentColor" stroke-width="2" fill="none" stroke-linecap="round" stroke-linejoin="round">
                <path d="M23 4v6h-6"></path>
                <path d="M1 20v-6h6"></path>
                <path d="M3.51 9a9 9 0 0 1 14.85-3.36L23 10M1 14l4.64 4.36A9 9 0 0 0 20.49 15"></path>
            </svg>
        </div>
    </div>
    
    <div id="hot-list-container">
        <!-- 列表内容由 JS 生成 -->
        <div class="loading-placeholder">正在获取热搜数据...</div>
    </div>
</div>



<style>

    /* ==========================================================================
    热搜组件样式 (MkDocs Material 原生风格适配版)
    ========================================================================== */

    /* 容器：使用主题默认背景和边框变量 */
    .hot-search-wrapper {
        margin-top: 30px;
        background: var(--md-default-bg-color); /* 跟随主题背景 */
        border-radius: 8px;
        /* 使用主题定义的阴影和边框颜色 */
        box-shadow: 0 4px 10px rgba(0,0,0,0.05);
        border: 1px solid var(--md-default-fg-color--lightest);
        overflow: hidden;
    }

    /* 顶部 Tab 栏 */
    .hot-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 12px 16px;
        /* 使用比背景稍浅/深一点的颜色，或直接透明 */
        background: var(--md-default-fg-color--lightest); 
        border-bottom: 1px solid var(--md-default-fg-color--lightest);
    }

    .hot-tabs {
        display: flex;
        gap: 8px;
    }

    /* Tab 按钮：核心修改 */
    .tab-btn {
        border: none;
        background: transparent; /* 默认无背景 */
        padding: 6px 14px;
        border-radius: 20px; /* 胶囊形状 */
        cursor: pointer;
        font-size: 0.9em;
        font-weight: 700;
        transition: all 0.2s ease;
        display: flex;
        gap: 6px;
        
        /* 默认文字颜色：使用主题的次要文字颜色 (灰色) */
        color: var(--md-default-fg-color--light); 
    }

    /* 鼠标悬停状态 */
    .tab-btn:hover {
        /* 悬停时文字变回主色，背景微微变亮 */
        color: var(--md-primary-fg-color); 
        background-color: var(--md-default-fg-color--lightest);
    }

    /* 选中状态 (Active) */
    .tab-btn.active {
        /* 背景变成主题的主色 (如 Teal, Blue, Purple) */
        background-color: var(--md-primary-fg-color);
        /* 文字变成白色 (Material Design 标准) */
        color: #fff; 
        box-shadow: 0 2px 6px rgba(0, 0, 0, 0.15);
    }

    /* 刷新按钮 */
    .hot-refresh {
        cursor: pointer;
        font-size: 1.2em;
        color: var(--md-default-fg-color--light); /* 跟随主题灰度 */
        padding: 4px;
        border-radius: 50%;
        transition: all 0.3s;
        display: flex;
        align-items: center;
        justify-content: center;
    }
    .hot-refresh:hover { 
        color: var(--md-primary-fg-color); /* 悬停变主题色 */
        background-color: var(--md-default-fg-color--lightest);
        transform: rotate(180deg); 
    }

    /* 列表容器 */
    #hot-list-container {
        padding: 5px 0;
        min-height: 200px;
    }

    .hot-list {
        list-style: none;
        padding: 0;
        margin: 0;
    }

    /* 列表单项 */
    .hot-item {
        display: flex;
        align-items: center;
        padding: 10px 20px;
        /* 虚线颜色跟随主题 */
        border-bottom: 1px dashed var(--md-default-fg-color--lightest);
        transition: background 0.2s;
    }

    .hot-item:hover {
        /* 悬停时背景微微变化 */
        background-color: var(--md-default-fg-color--lightest);
    }

    .hot-item:last-child {
        border-bottom: none;
    }

    /* 排名序号 */
    .hot-rank {
        width: 22px;
        height: 22px;
        line-height: 22px;
        text-align: center;
        border-radius: 4px;
        /* 默认背景：浅灰 */
        background: var(--md-default-fg-color--lightest);
        color: var(--md-default-fg-color--light);
        font-size: 0.8em;
        font-weight: bold;
        margin-right: 12px;
        flex-shrink: 0;
    }

    /* 前三名高亮 - 保持醒目颜色 */
    .hot-item:nth-child(1) .hot-rank { background: #ff4757; color: white; }
    .hot-item:nth-child(2) .hot-rank { background: #ff6b81; color: white; }
    .hot-item:nth-child(3) .hot-rank { background: #ffa502; color: white; }

    /* 链接样式 */
    .hot-link {
        text-decoration: none;
        color: var(--md-default-fg-color); /* 跟随正文颜色 */
        font-size: 0.95em;
        flex-grow: 1;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
        transition: color 0.2s;
    }

    .hot-link:hover {
        color: var(--md-primary-fg-color); /* 悬停变主题色 */
    }

    /* 热度值 */
    .hot-score {
        font-size: 0.75em;
        color: var(--md-default-fg-color--light); /* 灰色 */
        margin-left: 10px;
    }

    .loading-placeholder {
        text-align: center;
        padding: 40px;
        color: var(--md-default-fg-color--lighter);
    }

</style>


<script>



/**
 * =============================================================================
 * 模块四：多平台热搜组件 (Douyin/Toutiao/Quark)
 * =============================================================================
 */
function initHotSearch() {
    const container = document.getElementById('hot-list-container');
    const tabs = document.querySelectorAll('.tab-btn');
    if (!container || tabs.length === 0) return;

    // 防止重复绑定
    if (container.dataset.init === 'done') return;
    container.dataset.init = 'done';

    // API 配置
    const sources = {
        toutiao: { url: 'https://60s.viki.moe/v2/toutiao', name: '头条' },
        quark: { url: 'https://60s.viki.moe/v2/quark', name: '夸克' },
        douyin: { url: 'https://60s.viki.moe/v2/douyin', name: '抖音' }
    };

    let currentSource = 'toutiao'; // 默认显示

    // 获取并渲染数据
    async function loadData(sourceKey) {
        currentSource = sourceKey;
        container.innerHTML = '<div class="loading-placeholder">↻ 正在更新热搜...</div>';
        
        // 更新 Tab 样式
        tabs.forEach(btn => {
            if(btn.dataset.source === sourceKey) btn.classList.add('active');
            else btn.classList.remove('active');
        });

        try {
            const res = await fetch(sources[sourceKey].url);
            const json = await res.json();
            
            if (json.code === 200 && json.data) {
                renderList(json.data);
            } else {
                throw new Error('API Error');
            }
        } catch (e) {
            console.error(e);
            container.innerHTML = '<div class="loading-placeholder">❌ 获取失败，请稍后重试</div>';
        }
    }

    // 渲染 HTML 列表
    function renderList(data) {
        // 部分接口返回的数据结构可能不同，做个简单的兼容处理
        // 抖音/头条通常是 data 数组，Quark 也是
        const list = Array.isArray(data) ? data : (data.data || []);
        
        if (list.length === 0) {
            container.innerHTML = '<div class="loading-placeholder">暂无数据</div>';
            return;
        }

        let html = '<ul class="hot-list">';
        // 只取前 15 条，避免页面太长
        list.slice(0, 15).forEach((item, index) => {
            // 字段兼容：有的叫 title/url，有的叫 keyword/link
            const title = item.title || item.keyword || item.word;
            const url = item.url || item.link;
            const hot = item.hot_value || item.hot || '';

            // 如果没有链接，就用搜索链接代替
            const finalUrl = url ? url : `https://www.baidu.com/s?wd=${encodeURIComponent(title)}`;

            html += `
                <li class="hot-item">
                    <span class="hot-rank">${index + 1}</span>
                    <a href="${finalUrl}" target="_blank" class="hot-link">${title}</a>
                    ${hot ? `<span class="hot-score">🔥 ${formatHot(hot)}</span>` : ''}
                </li>
            `;
        });
        html += '</ul>';
        container.innerHTML = html;
    }

    // 格式化热度数值 (1234567 -> 123.4w)
    function formatHot(num) {
        if (!num) return '';
        // 如果包含中文直接返回
        if (typeof num === 'string' && num.includes('万')) return num;
        
        const n = parseInt(num);
        if (isNaN(n)) return num;
        
        if (n > 10000) return (n / 10000).toFixed(1) + 'w';
        return n;
    }

    // 绑定 Tab 点击事件
    tabs.forEach(btn => {
        btn.onclick = () => loadData(btn.dataset.source);
    });

    // 绑定刷新函数到 window (以便 HTML 中的 onclick 能调用)
    window.refreshHotSearch = () => loadData(currentSource);

    // 初始加载
    loadData(currentSource);
}

initHotSearch();

</script>